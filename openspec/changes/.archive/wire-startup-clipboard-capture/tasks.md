# 任务：打通启动剪贴板捕获

- [x] 新建 OpenSpec change
- [x] 启动时立即读取系统剪贴板
- [x] 新文本进入快速粘贴顶部并选中
- [x] 已存在文本提升到顶部且不重复
- [x] 增加手动“读取剪贴板”按钮
- [x] 默认启动为小快捷面板窗口
- [x] 小面板响应式隐藏详情栏和批量管理
- [x] 放大窗口后恢复完整功能面板
- [x] 支持搜索和 tag 快速筛选叠加
- [x] 列表改为虚拟滚动，降低大量历史项的 DOM 渲染压力
- [x] 搜索输入增加 debounce，减少输入时重复全量过滤
- [x] 记录当前存储/检索边界：历史进入本地 SQLite，检索走 FTS5 + limit/cursor
- [x] 抽象 `ClipboardRepository`、`SearchIndex`、`SettingsStore`、导入导出、同步、外部工具契约
- [x] 接入 SQLite + FTS5 最小闭环，替代历史数据长期保存在前端
- [x] 支持软删除和定期清理配置
- [x] 快捷面板支持多选模式、快速删除和聚合复制
- [x] macOS 快捷面板接入 `tauri-nspanel`，使用 NSPanel 覆盖全屏应用 Space
- [x] 快捷键唤起显示前先探测当前输入控件位置，失败后再按鼠标所在屏幕和右侧兜底定位
- [x] 二次触发快捷键直接隐藏快捷面板
- [x] 失焦后延迟自动隐藏，避免面板长时间挡住当前应用
- [x] 保证快捷列表虚拟滚动可滚动并能触底加载更多
- [x] 非 macOS 快捷键唤起默认使用右侧侧拉式固定浮层
- [x] 系统状态栏托盘菜单可触发同一个快捷面板
- [x] 悬浮面板默认置顶，macOS NSPanel 不抢焦点显示，点击/搜索时可成为 key window
- [x] 数字键和 Enter 接入原生快速粘贴命令，粘贴前隐藏/释放面板
- [x] Agent 快速访问契约聚焦 `clipboard.capture/search/copy/update/delete/export/import`，不进入 AI 配置面板
- [x] 补齐 Tauri window/global-shortcut capability 权限，避免快捷键触发链路被权限拦截
- [x] 将默认快捷键改为 macOS `Control+V`，并移到 Rust 原生侧注册
- [x] 拖拽改为显式调用 `startDragging()`，避免透明无边框窗口拖不动
- [x] 小浮层下所有内容页统一使用快捷列表样式，避免切换到完整管理行导致卡顿和重叠
- [x] 快捷列表移除 URL 来源头像，默认显示字号和图标尺寸压到 12px 级别
- [x] 支持本地日志文件和前端错误兜底
- [x] 日志改为标准 JSONL，提供分级查询接口并在设置中可检索
- [x] 通过 `pnpm build`
- [x] 通过 `cd src-tauri && cargo check`
- [x] 启动 `pnpm tauri dev` 供用户查看
